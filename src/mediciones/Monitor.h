using namespace std;

class Monitor {
   private:
      // Datos miembro de la clase "pareja"
      Channel canales[];
   public:
      // Funciones miembro de la clase "pareja"
      void setCanales(int numCanales[]);
      char* getCanales();

};
